# Architecture

Default:

- one Rails app
- root domain marketing
- app subdomain product

Start simple. Add more infrastructure only when the MVP needs it.

For local development, use the marketing root on `localhost` and the product area on `app.lvh.me`.
