Read `docs/` and `AGENT.md`.

If priority is unclear, stop and ask.

Build the next highest-priority feature using Rails conventions.

Ask before inventing product behavior.

Prefer the simplest implementation that meaningfully validates the idea.

Commit changes when the task is complete.
